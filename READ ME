The corpus used is reuters of NLTK

trie.cpp: Handles wild card queries and spelling correction. No dependencies; can be complied and run directly.

VectorSpaceModel.ipynb: IPython notebook format. Open with jupyter notebook. Also need to install numpy, nltk, gensim and all its dependencies. Easiest way is to install anaconda and use conda install.

tfindex.p: Persisted file of the tf index dictionary.

save.p: persisted file of the inverted index.

dict.csv: Dictionary of IDF values saved as a csv file.




